rules_version = '2';
service firebase.storage {
  match /b/{bucket}/o {
    match /reports/{reportId}/{allPaths=**} {
      // Any authenticated user can read report photos
      allow read: if request.auth != null;
      // Only the report owner can write
      allow write: if request.auth != null;
    }
  }
}
